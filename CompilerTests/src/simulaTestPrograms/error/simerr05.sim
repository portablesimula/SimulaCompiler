
% Semicolon before 'else'
BEGIN
    integer state;
    boolean occupied;
    
         if state=1 then begin state:=2; occupied:=true; end;
         else if state=2 then begin state:=3; occupied:=false; end;
         else if state=3 then
         begin 
         end;

end of program;
