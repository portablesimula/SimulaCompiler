% CallProcedureConnected
begin
  ref(A) x;
  integer i;

  Class A(p1,p2); integer p1; real p2;
  begin
    integer Procedure P(p1,p2);
      integer p1; real p2;
    begin
      P:=p1;
    end;
  end;
  
  inspect x do
  i:=P(4,3.14); 
  
end;  