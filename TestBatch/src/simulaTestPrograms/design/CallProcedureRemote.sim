% CallProcedureRemote
begin
  ref(A) x;
  integer i;

  Class A(p1,p2); integer p1; real p2;
  begin
    integer Procedure P(p1,p2);
      integer p1; real p2;
    begin
      P:=p1;
    end;
  end;
  
  i:=x.P(4,3.14); 
  
end;  