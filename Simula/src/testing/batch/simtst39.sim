
% ===============================================   Main   program =============
%  PROGRAM NAME:   Simula a.s. - SIMULA TEST NO 39
%
%  PURPOSE:     Check that a formal parameter is global to the procedure body.
%
%  MYH-NOTE: DEVIATIONS FROM 'SIMULA STANDARD':
%               Parameters and declarations at the outmost level in classes and
%               procedures form a common namespace. This means that a local
%               declared can not have the same name as a parameter.
%               The reason is that Java has the same restriction.
%
%  WRITTEN BY:  BEH   23/07-86, MODIFIED:  MYH  30/5-2018
% ==============================================================================
BEGIN
   Boolean found_error ;

   integer i ;

   procedure P (pi) ;
     name pi ;
     integer pi ;
   begin
     Integer i ;
     i := 5 ;
   end ;

   Outtext ("--- START Simula a.s. TEST 39") ;
   Outimage ;
   Outtext ("--- Check that the formal parameters are global to the") ;
   Outimage ;
   Outtext ("--- procedure body.") ;
   Outimage ; Outimage ;

   P (i) ;

   if i <> 0 then
   begin
      Outtext ("*** error : Wrong variable used in procedure body.") ;
      Outimage ;
      found_error := true
   end ;

   IF found_error THEN
   ELSE
   BEGIN
      Outtext ("--- NO ERRORS FOUND IN THIS TEST") ;
      Outimage
   END ;

   Outtext ("--- END Simula a.s. TEST 39") ;
   Outimage
 END
