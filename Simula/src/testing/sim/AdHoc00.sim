SIMULATION
begin integer i,klara; real tid,slut;
                  
   ref(link) elt;

   process class kund;
   begin integer efter; real intid; array servtid(1:2);
                       
   end;

   elt:-new kund();
%   process class station(nr,position); integer nr,position;
%   inspect kundq(nr).first when kund do
   inspect elt when kund do
   begin tid:=this kund.intid;
   end

end;
