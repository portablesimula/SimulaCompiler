begin
  integer array A(1:45,4:27);
  Text T,U="abraCadab";
  Integer i,ci=16R6E4;
  Boolean b;
  Character c;
  real r,x,y;
  long real w,q;
  
  Infile class MyInfile;
  Begin
     integer procedure inRom;
     Begin 
     end;
  end;
  
  i := i+4;
  
  b := T.constant;
  
  outtext(if b then "true" else "false"); outimage;
  
  c := decimalmark(',');
  T :- upcase(T);
  T :- lowcase(T);
  r := arctan2(y,x); 
  
  i := sourceline+47;

  w := maxlongreal;
  q := minlongreal;

  x := maxreal;
  y := minreal;

  i := maxrank;
  i := maxint;
  i := minint;

  T :- simulaid; 

  i := lowerbound(A,1);
  i:=  upperbound(A,2);
  
  ci := rem(i,4); 

  error("AbRaCadeb");
  
end of program;
