SIMULATION
begin
   integer x,y,z;
   boolean bb;
   character ch;
   text t;
   ref(Process) p,a,b,c;
   

%   for ch:=inchar while ch ne ' 'do t.putchar(ch);
   
%   for bb:=true, false, x>y while z<1 do z:=z+12;
   
   t:-blanks(12);
   for t:="abra","ca","dabra" do outtext(t); outimage;
   
   for p:-a,b,c while p.evtime<1 do z:=z+12;

end;
