Class B(t,i,r); value t; Text t; integer i; real r;
BEGIN
   Text txt;
   Ref(A) ra;
   
   Procedure P(t,i,r); value t; name r; text t; integer i; real r;
   Begin 
      integer ukjent;
   end;    

   Class A(navn);  VALUE navn;  Text navn;
   BEGIN
   END of class A;

   txt :- Copy("ABRACADAB");
   ra  :- New A(txt);
   outtext(ra.navn); outimage;
   txt := "ADABRA";
   outtext(ra.navn); outimage;

END
