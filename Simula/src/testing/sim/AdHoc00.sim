BEGIN

   ref(A) x;

   Class A; virtual: procedure P;
   begin integer i;
      Procedure P(line); integer line;
      Begin
            outtext("Call P -- Line="); outint(line,0); outimage;
      End;
      i:=sourceline;
   end;
   
   x:-new A;
   x.P(sourceline);

END
