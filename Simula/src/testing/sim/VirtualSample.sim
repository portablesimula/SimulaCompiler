
%QUALIFIEDNAMES

begin
   real x;
   class A; virtual: Procedure P,Q; Label L; Switch S;
   begin
      real y;
      procedure P(x,i,r); ref(A) x; integer i; real r;
      begin end;
      
      L1:P(new B(),3.5,7.9);
      L2:Q(x+y);
      goto S(2);
   end;
   
   A class B;
   begin procedure P(x,i,r); ref(B) x; integer i; real r;
         begin end;
         procedure Q(r); real r;
         begin end;
         switch S:=L1,L2;
         switch T:=L2,L1;
         goto T(2);
   end;      
end;
