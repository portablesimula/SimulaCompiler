Simset class Issue37a;
begin
	ref(quant) QQQ;
	
	link class quant;
	begin
		 text TTT;
	end;

	class parspec;
	begin ref(head) import;
          ref(quant) q;
          for q:-import.first do;
	end;
	
	procedure TRC(q); ref(quant) q;
	begin end;
end;